{
  "name": "diwan-alqabila-app",
  "version": "1.0.0",
  "private": true,
  "description": "ديوان القبيلة Android app wrapper",
  "scripts": {
    "build": "echo 'Remote website app - no local web build required'",
    "android": "npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.0.0"
  }
}
